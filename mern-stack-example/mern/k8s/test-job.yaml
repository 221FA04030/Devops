apiVersion: batch/v1
kind: Job
metadata:
  name: test-job
spec:
  template:
    spec:
      containers:
        - name: migration
          image: busybox
          command: ["sh", "-c", "echo Database migration completed"]
      restartPolicy: Never